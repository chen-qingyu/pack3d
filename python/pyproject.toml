[build-system]
requires = ["setuptools>=68", "wheel>=0.43"]
build-backend = "setuptools.build_meta"

[project]
name = "pack3d"
version = "0.1.0"
description = "Python SDK for the pack3d solver."
readme = "readme.md"
requires-python = ">=3.9"

[tool.setuptools]
packages = ["pack3d"]
